version: v1
services:
  - name: mybb
    ports:
      - port: 8080
        protocol: HTTP
    env:
      - key: DATABASE_URL
        from: service
        service: mybb-db
    build:
      dockerfile: Dockerfile
